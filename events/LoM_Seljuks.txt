namespace = lom_seljuks
normal_or_historical_nations = yes

# End of the Seljuk State
country_event = {
	id = lom_seljuks.1
	title = lom_seljuks.1.t
	desc = lom_seljuks.1.d
	picture = RAMADAN_eventPicture
	
	fire_only_once = yes
	
	trigger = {
		tag = SJK
		has_regency = no
		has_consort_regency = no
		NOT = {
			dynasty = "Seljuk"
		}
	}
	
	option = {
		name = lom_seljuks.1.a
		# make this dynamic depending whether owns Persia or not
		change_tag = IRQ
		swap_free_idea_group = yes
		set_primary_culture = al_iraqiya_arabic
		add_accepted_culture = turko_iranian
		410 = {
			move_capital_effect = yes
		}
		define_ruler = {
			ADM = 3
			DIP = 3
			MIL = 4
			fixed = yes
		}
	}
}
